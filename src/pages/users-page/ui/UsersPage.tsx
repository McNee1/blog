import { UserManager } from '@/features';
import { PageManager } from '@/widgets';

const UsersPage = () => {
  return (
    <PageManager>
      <UserManager />
    </PageManager>
  );
};

export default UsersPage;
